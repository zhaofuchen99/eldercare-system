<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zfc.eldercare.core.mapper.UserMapper">

    <sql id="Base_Column_List">
        id, phone, password, real_name, gender, birth_date, height, avatar,
        emergency_contact, member_level, points, status, role, create_time, update_time, deleted
    </sql>

    <select id="selectByPhone" resultType="com.zfc.eldercare.core.entity.User">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `user`
        WHERE phone = #{phone} AND deleted = 0
    </select>

    <select id="selectById" resultType="com.zfc.eldercare.core.entity.User">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `user`
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByIds" resultType="com.zfc.eldercare.core.entity.User">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `user`
        WHERE deleted = 0 AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <insert id="insert" parameterType="com.zfc.eldercare.core.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (phone, password, real_name, gender, birth_date, height, avatar,
                            emergency_contact, member_level, points, status, role)
        VALUES (#{phone}, #{password}, #{realName}, #{gender}, #{birthDate}, #{height}, #{avatar},
                #{emergencyContact}, #{memberLevel}, #{points}, #{status}, #{role})
    </insert>

    <update id="updatePassword">
        UPDATE `user`
        SET password = #{password}
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updatePoints">
        UPDATE `user`
        SET points = points + #{delta}
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deductPoints">
        UPDATE `user`
        SET points = points - #{amount}
        WHERE id = #{id} AND points &gt;= #{amount} AND deleted = 0
    </update>

</mapper>
