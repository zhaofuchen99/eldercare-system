<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zfc.eldercare.core.mapper.AiSessionMapper">

    <sql id="Base_Column_List">
        id, user_id, session_name, create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="com.zfc.eldercare.core.entity.AiConversationSession" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_conversation_session (user_id, session_name)
        VALUES (#{userId}, #{sessionName})
    </insert>

    <select id="selectById" resultType="com.zfc.eldercare.core.entity.AiConversationSession">
        SELECT
        <include refid="Base_Column_List"/>
        FROM ai_conversation_session
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectPageByUserId" resultType="com.zfc.eldercare.core.entity.AiConversationSession">
        SELECT
        <include refid="Base_Column_List"/>
        FROM ai_conversation_session
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY update_time DESC, id DESC
    </select>

    <update id="updateName">
        UPDATE ai_conversation_session
        SET session_name = #{sessionName}
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="touch">
        UPDATE ai_conversation_session
        SET update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="delete">
        UPDATE ai_conversation_session
        SET deleted = 1
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
