<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zfc.eldercare.core.mapper.RoleMapper">

    <sql id="Base_Column_List">
        id, role_code, role_name, description, status, create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="com.zfc.eldercare.core.entity.Role" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role (role_code, role_name, description, status)
        VALUES (#{roleCode}, #{roleName}, #{description}, #{status})
    </insert>

    <select id="selectById" resultType="com.zfc.eldercare.core.entity.Role">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByIds" resultType="com.zfc.eldercare.core.entity.Role">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role
        WHERE deleted = 0 AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectByCode" resultType="com.zfc.eldercare.core.entity.Role">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role
        WHERE role_code = #{roleCode} AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectIdByCode" resultType="java.lang.Long">
        SELECT id
        FROM role
        WHERE role_code = #{roleCode} AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectPage" resultType="com.zfc.eldercare.core.entity.Role">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (role_code LIKE CONCAT('%', #{keyword}, '%') OR role_name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY id
    </select>

    <select id="selectAll" resultType="com.zfc.eldercare.core.entity.Role">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role
        WHERE deleted = 0
        ORDER BY id
    </select>

    <update id="update" parameterType="com.zfc.eldercare.core.entity.Role">
        UPDATE role
        <set>
            <if test="roleCode != null">role_code = #{roleCode},</if>
            <if test="roleName != null">role_name = #{roleName},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="delete">
        UPDATE role
        SET deleted = 1
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
